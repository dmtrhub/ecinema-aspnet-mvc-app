@model IEnumerable<Producer>

@{
    ViewData["Title"] = "Producers";
}

<div class="row">
    <div class="col-md-8 offset-md-2">
        <table class="table">
            <thead>
                <th>@Html.DisplayNameFor(model => model.PictureUrl)</th>
                <th>@Html.DisplayNameFor(model => model.FullName)</th>
                <th>@Html.DisplayNameFor(model => model.Bio)</th>
                <th>Action</th>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            <img class="rounded-circle" src="@item.PictureUrl" alt="@item.FullName" style="max-width: 150px"/>
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(itemModel => item.FullName)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(itemModel => item.Bio)
                        </td>
                        <td class="align-middle">
                            <a class="btn btn-outline-primary"><i class="bi bi-pen-fill"></i>Edit</a> |
                            <a class="btn btn-outline-info"><i class="bi bi-info-square-fill"></i>Details</a> |
                            <a class="btn btn-outline-danger"><i class="bi bi-trash-fill"></i>Delete</a> 
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>